# Conspectus

**Conspectus** — это веб-приложение для хранения, организации и быстрого поиска конспектов и заметок в формате Markdown.

Находится в разработке.

На текущий момент доступен функционал поиска и просмотра заметок, созданных вручную и размещенных в подкаталогах `/src/markdown`

[Пример работы приложения на git-pages](https://tatnik.github.io/conspectus/)

---

## 🚀 Быстрый старт

### Локально (pnpm)

```bash
pnpm install
pnpm start
```

### Локально (npm)

```bash
npm install
npm run start
```

### В Docker

```bash
npm run docker:build
npm run docker:up
```

Проект будет доступен по адресу [http://localhost:3000/conspectus](http://localhost:3000/conspectus)

---

## 📦 Структура проекта

- `/src` — исходный код приложения
- `/src/markdown` — файлы конспектов
- `/public` — статические файлы (иконки, favicon, etc.)
- `/public/jpg/md` — файлы изображений для разделов и заметок (используются на индексных страницах)
- `.testCreater/` — генератор тестов
- `.github/` — workflows для CI/CD
- `README.md` — этот файл
- и др.

---

## ⚙️ Основные команды

| Команда       | Назначение                 |
| ------------- | -------------------------- |
| `pnpm start`  | Запуск в режиме разработки |
| `pnpm build`  | Сборка проекта             |
| `pnpm deploy` | Деплой на git_pages        |
| `pnpm lint`   | Проверка кода линтером     |
| `pnpm test a` | Запуск тестов              |

---

## 📝 Как устроен проект

- **Frontend:** TypeScript, CRA
- **Данные:** Markdown-файлы в каталоге проекта
- **CI/CD:** GitHub Actions для автоматических проверок

---

## 🧪 Тестирование

В проекте используется **Jest** совместно с **@testing-library/react** для тестирования React-компонентов и бизнес-логики.
Для поддержки TypeScript и специфики CRA используется набор утилит и настройка окружения.

### Запуск тестов

> `pnpm test a`

Для CI (без интерактивного режима, с жестким завершением при ошибках):

> `pnpm run test:ci`

### Тестовые утилиты

В проекте определён собственный **renderWithProviders**, который автоматически оборачивает компонент в необходимые контексты (Provider, AppThemeProvider, etc.), что позволяет тестировать компоненты изолированно и без лишней настройки в каждом тесте.

### Моки и заглушки

Для мокирования глобальных функций (например, fetch, useParams, хранилища контекста) используются моки через Jest и отдельные мок-контексты, передаваемые через renderWithProviders.

Для тестов, связанных с асинхронными действиями, применяется jest.fn() и кастомные моки.

### Особенности

Тесты автоматически прогоняются при пуше в репозиторий (GitHub Actions).

Для совместимости с TypeScript и CRA используются @types/jest и ts-node.

### Генерация тестов

Для ускорения процесса написания типовых тестов в проекте используется собственный скрипт автогенерации на основе файлов поведения (**\*.behavior.mj**).

Сценарии тестирования описываются декларативно в виде массива в файле ComponentName.behavior.ts, далее по ним автоматически формируются шаблонные тесты. В качестве шаблона теста используется файл **.testCreater/testTemplate.txt**

#### Как сгенерировать тесты

Выполните команду:

> `node .testCreator/scripts/testCreator.mjs`

Скрипт создаст/обновит тестовые файлы для всех компонентов, у которых есть соответствующий .behavior-файл.

#### Формат .behavior-файлов

```js
export default {
  // Необязательные: массив импортов и заголовочных инструкций (добавляются в тест)
  imports: [ 'import { screen } from "@testing-library/react";' ],

  // Необязательный: массив строк, помещаемый перед строкой импорта тестируемого компонента
  header: [ 'jest.mock(...)' ],

  // Необязательный: объект с моковоым контекстом по умолчанию для всех тестов
  mockContext: {},

  // Необязательный: объект с props по умолчанию для всех тестов
  props: { /* ... */ },

  // Необязательный: массив строк, помещаемый после describe до всех тестов
  describe_header: [
    'beforeEach(() => {... });',]

  // Массив сценариев тестирования
  tests: [
    {
      it: 'описание сценария (что должно происходить)',
      async: true | false, // нужно ли использовать async/await в тесте
      test_header: [ 'jest.mock(...)' ], // (опционально) массив строк с командами, выполняемыми перед тестом
      mockContext: {}, // (опционально) моковоый контекст только для этого теста
      props: { /* ... */ }, // (опционально) props только для этого теста
      steps: `
        // Код теста, обычно в формате Testing Library/Jest
        expect(screen.getByText('...')).toBeInTheDocument();
      `,
    },
    // дополнительные сценарии
  ],
};
```

**Примечание**: Итоговые props для теста получаются путем объединения props теста поверх props для всех тестов.

#### Пример файла behavior:

```js
export default {
  imports: [`import { screen } from '@testing-library/react';`],
  // Дефолтные props для всех тестов, где явно не указаны свои props
  props: {
    navLinkArray: [
      { id: 1, name: 'Link1', path: '/' },
      { id: 2, name: 'Link2', path: '/about' },
    ],
    classNameList: 'nav',
    renderProps: '__INLINE_FUNC__:(val) => val.name',
  },
  tests: [
    {
      it: 'отрисовывает все navLinkArray как ссылки',
      async: false,
      steps: `
        const links = screen.getAllByRole('link');
        expect(links.length).toBe(2);
        expect(links[0]).toHaveTextContent('Link1');
        expect(links[1]).toHaveTextContent('Link2');
      `,
    },
    {
      it: 'вызывает renderProps для каждого элемента',
      async: false,
      props: {
        renderProps: '__JEST_FN__',
      },
      steps: `
        expect(renderProps).toHaveBeenCalledTimes(2);
      `,
    },
    {
      it: 'применяет класс активного элемента для активного пути',
      async: false,
      steps: `
        const items = screen.getAllByRole('listitem');
        expect(items[0].className.split(' ')).toContain('active');
      `,
    },
  ],
};
```

    Важно: После генерации тесты можно доработать вручную.

    Автоматизация не заменяет написание тестов для сложной логики, но позволяет быстро покрыть типовые сценарии.

    В случае ручных изменений тестового файла переименуйте или удалите соответствующий .behavior-файл.

---

## ❓ FAQ

- **Как добавить новый раздел в заметки?**

  В папку проекта markdown добавьте папку (наименование папки - латиницей, без пробелов)
  В файл markdown/index.md добавьте ссылку на новый раздел:
  `## [Это мой новый раздел](/new)`
  В добавленной папке создайте файл index.md. В этот файл вы будете в дальнейшем добавлять ссылки на новые заметки.

- **Как добавить новую заметку?**

  Скопируйте Markdown-файл в папку подходящего раздела.
  В индексный файл раздела добавьте ссылку на новую заметку: `## [Это моя новая заметка](/new/my_conspect)`

- **Как добавить изображение для нового раздела?**

  В папке `public/jpg/md` разместите файл изображения (220 х 220 px) в формате **jpg**.

  Имя файла должно совпадать с именем раздела + суффикс `-220`.

  Например, для раздела `js` файл с изображением называется `js-200.jpg`

  см. еще примеры в папке проекта `public/jpg/md`

- **Как добавить изображение для новой заметки?**

  В папке `public/jpg/md/<имя раздела>` разместите файл изображения (220 х 220 px) в формате **jpg**.

  Имя файла должно совпадать с именем файла заметки + суффикс `-220`.

  Например, для заметки `js_base.md`, расположенной в папке `src/markdown/js` файл с изображением называется `js_base-200.jpg` и находится в папке `public/jpg/md/js/`

  см. еще примеры в папках проекта `public/jpg/md/*`

- **Как добавить изображение в текст заметки?**

  В папке `public/jpg/` разместите файл изображения в формате **jpg**.

  В тексте заметки добавьте ссылку на изображение. Например:
  `![forking-workflow](/conspectus/jpg/forking-workflow.jpg)`

- **Как задеплоить на GitHub Pages?**

  запустите скрипт `pnpm deploy`

- **Проблемы со стилем кода?**

  в package.json добавлены скрипты для различных проверок и форматирования

---

## 🤝 Контрибьютинг

1. Сделайте fork репозитория
2. Создайте новую ветку для своей фичи (`git checkout -b feature/my-feature`)
3. Протестируйте изменения локально
4. Создайте Pull Request

Перед PR обязательно прогоняйте линтер и тесты!

---

## 📄 Лицензия

[MIT](./LICENSE)

---

**Автор:** [Татьяна Попова](https://github.com/tatnik)
Вопросы и предложения — в Issues.
