export type TypeNavLink = {
  id: number;
  name: string;
  path: string;
};

export type TypeNavArray = TypeNavLink[];
export type TypePartNavArray = TypeNavArray[];

export type TypeNavLinkOrNull = TypeNavLink | null;
